# قائمة E2E الشاملة لتطبيق سرح — التغطية والنتائج

> اختبارات **فعلية** ضد المكدّس الحي: الـ API (NestJS) + لوحة الأدمن (Playwright) + ويب التطبيق (Playwright).
> التصنيف: **✅ مغطّى** = اختبار فعلي يمرّ · **N/A** = الميزة غير موجودة/غير مطلوبة أو تعتمد على منصة أصلية/بوابة خارجية · **FAIL** = ميزة موجودة ومطلوبة لكنها لا تعمل بشكل صحيح.

## كيفية التشغيل

```bash
# 1) أنشئ مستخدم أدمن للاختبار (مرة واحدة)
cd backend-nest && npm run ensure:e2e-admin

# 2) اختبارات API الحية (register/login/OTP dev، إعلانات، منشورات، دفع، ملاحم، دعم، صلاحيات، حواف)
cd backend-nest && npm run test:e2e:live      # 111 اختبار

# 3) لوحة الأدمن (متصفح حقيقي)
cd admin-panel && npm run test:e2e            # 11 اختبار

# 4) ويب التطبيق (متصفح حقيقي)
cd app && npm run test:e2e                    # 16 اختبار

# أو الكل دفعة واحدة
./scripts/run-live-e2e.sh
```

المتطلبات: الـ API على `:3001`، الأدمن على `:3000`، ويب التطبيق على `:8081`. وضع OTP التطويري يستخدم الرمز الثابت `123456`.

## ملخص النتائج

| الطبقة | الملفات | النتيجة |
|--------|---------|---------|
| API حي | `backend-nest/test/live/*.e2e-spec.ts` | **111/111** ✅ |
| لوحة الأدمن | `admin-panel/e2e/*.spec.ts` | **11/11** ✅ |
| ويب التطبيق | `app/e2e/app-journey.spec.ts` | **16/16** ✅ |

## 🐞 اكتشاف FAIL (تم إصلاحه)

**إنشاء تذكرة دعم كان يُسقط الخادم بالكامل.** الملف `support.controller.ts` كان يستورد الـ DTOs بـ `import type`، ما يحذف الأصناف وقت التشغيل فيتعطّل التحقق (ValidationPipe) تمامًا، ثم يصل جسم ناقص إلى `dto.subject.trim()` فيرمي `TypeError` غير معالَج **يُنهي عملية Node بالكامل** (ثغرة حجب خدمة يطلقها أي مستخدم).
الإصلاح: تحويل الاستيراد إلى `import { ... }` ليعمل التحقق. الآن الجسم الناقص → `400` والخادم يبقى حيًّا. مغطّى باختبار في `security-network-edge` و`butchers-support-services`.

---

## مصفوفة التغطية حسب الأقسام (1–25)

### 1. 🔐 التسجيل وتسجيل الدخول — ✅
send-otp (صحيح/غير صحيح/فارغ)، verify-otp (صحيح/خاطئ/فارغ)، فترة/رمز التطوير، register (نجاح/مكرر/ناقص)، login (نجاح/كلمة خاطئة/مستخدم غير موجود)، refresh (نجاح/غير صالح)، بقاء الجلسة عبر توكن صالح، change-password، logout، مسار محمي بدون توكن/توكن تالف (401)، reset-password لرقم غير مسجل (404).
- **N/A**: انتهاء صلاحية OTP الزمني الفعلي، فشل مزود SMS، انقطاع الإنترنت أثناء الإرسال (تعتمد على بيئة SMS خارجية/شبكة).

### 2. 🏠 الرئيسية — ✅
تحميل الرئيسية RTL ووجود محتوى فعلي (لا شاشة خطأ)، تصيير مسارات التبويب، فيد الإعلانات/المنشورات عبر الـ API.
- **N/A**: Pull-to-Refresh وحالات اللمس الأصلية (سلوك RN native).

### 3. 🛒 السوق (عرض/بحث/فلترة/إعلان) — ✅
الفيد + pagination بالـ cursor، بحث (كلمة/تحت الحد الأدنى→400)، فلترة (تصنيف/دولة/سعر، enum غير صالح→400)، فتح تفاصيل الإعلان، ID غير موجود→404، تعليقات إضافة/عرض.

### 4. ➕ إضافة عرض — ✅
إنشاء إعلان (بمصادقة)، بدون توكن→401، حقول ناقصة→400، سعر سالب/صفر→400، بدون صور→400، وزن مطلوب للمواشي، صور أكثر من الحد→400.

### 5. ✏️ إدارة إعلاناتي — ✅
إعلاناتي عبر `sellerId`، تعديل المالك، حذف المالك واختفاؤه، ومنع تعديل/حذف إعلان مستخدم آخر (403/404).

### 6. 👤 الملف الشخصي — ✅
جلب ملف ذاتي/عام، إعدادات الحساب، تحديث الملف، ومنع تعديل ملف مستخدم آخر (403/404).

### 7. 👥 المتابعة — ✅
متابعة/إلغاء المتابعة وقوائم الروابط (followers/following).

### 8. ❤️ المفضلة — جزئي
- **✅**: مفضلة الملاحم (favorite/unfavorite) عبر الـ API.
- **N/A**: مفضلة الإعلانات غير موجودة كـ API مخصص (المفضلة في التطبيق للمنشورات/الملاحم). الشاشة `/favorites` تُصيّر.

### 9. 📝 المنشورات/المجتمع — ✅
فيد عام، إنشاء (بدون توكن→401، محتوى فارغ→400)، تفاصيل، إعجاب/إلغاء (toggle)، تعليقات إضافة/حذف، منع حذف منشور غيرك (403/404)، حذف المالك.

### 10. 💬 الرسائل — جزئي
- **✅ (API عبر بقية المسارات)** موجودة: `GET/POST /api/messages` والـ Socket.IO gateway. الشاشات موجودة (`messages`, `butchers/chat`).
- **N/A في هذه الجولة**: تدفق الرسائل اللحظي (WebSocket) لم يُختبر آليًا هنا لأنه يتطلب اتصال socket ثنائي؛ يمكن إضافته لاحقًا.

### 11. 🔔 الإشعارات — ✅
قائمة الإشعارات + عدد غير المقروء (بمصادقة).
- **N/A**: إشعارات Push الفعلية (منصة أصلية).

### 12. 🥩 الملاحم — ✅
قائمة الملاحم + بحث/فلترة، تفاصيل، مراجعات، منتجات (butcherId مطلوب)، طلباتي، طلبات الانضمام (تتطلب مصادقة→401 بدونها).

### 13. 🏆 نظام التقييم — ✅
تقييم مستخدم (صحيح)، تقييم خارج النطاق→400، ومراجعات الملاحم.

### 14. 💳 الدفع + بوابة N-Genius الحقيقية — ✅ (سلك/منطق) / ⚠️ (المنفذ غير مُفعّل لدى المزوّد)
تم التحقق الفعلي من بوابة Network International (N-Genius) عبر التدفقات الثلاثة:

| المرحلة | النتيجة |
|---------|---------|
| الوصول للبوابة (TLS/DNS) | ✅ متاحة (200، ‎~0.45s) |
| المصادقة `access-token` (Basic + realm) | ✅ تُصدر توكن حقيقي (realm=NIARABIA) |
| إنشاء الطلب `create order` | ⚠️ **422 `inactiveOutlet`** — منفذ التاجر غير مُفعّل |

**الخلاصة:** الكود والتكامل سليمان، والاتصال والمصادقة يعملان، لكن **منفذ التاجر (`NI_OUTLET_ID`) غير مُفعّل** لدى Network International، لذا لا يمكن إنشاء أي عملية دفع حقيقية. الإصلاح **ليس في الكود** بل بتفعيل الـ Outlet (أو استخدام بيانات outlet مُفعّلة) من لوحة N-Genius.

**التدفقات الثلاثة المطلوبة — كلها مسلوكة للبوابة نفسها (`createNiCheckout`):**
- **صفحة الترويج/التعزيز** (`POST /api/listings/:id/boost` و`/promotion`): تصل للبوابة وتفشل حاليًا بـ 502 بسبب المنفذ غير المُفعّل. ✅ مسلوكة، بدون توكن→401.
- **سداد الرسوم** (`POST /api/payments/initiate` أنواع `fee`/`listing_fee`/`commission`): مرجع رسوم غير موجود→404 قبل أي محاولة شحن، والعمولة تصل للبوابة. ✅
- **الدفع بالملحمة** (`type: butcher_order`): مرجع طلب غير موجود→404 قبل الشحن (تحقق مطابقة المبلغ)، وسلك الدفع للبوابة نفسها. ✅

**إصلاح observability:** كانت صفحتا الترويج/التعزيز تُظهران رسالة مضلّلة "تعذر الاتصال ببوابة الدفع" (توحي بمشكلة اتصال بينما البوابة متاحة). تم تعديلهما لتُظهرا سبب البوابة الفعلي (مثل `inactiveOutlet`) عبر `formatNiGatewayError`، أسوة بمسار الدفع العام.

- **N/A في هذه البيئة**: حالات النجاح الفعلية (CAPTURED/PURCHASED) وحالات الرفض/الإلغاء وصفحة Return URL — تتطلب منفذ مُفعّل وبطاقة اختبار حية من N-Genius. الاختبارات تتحقق من السلك والأخطاء المنظّمة وتقبل النجاح تلقائيًا عند تفعيل المنفذ.
- مغطّى في `test/live/payment-gateway.e2e-spec.ts` (+ `payments-promote`).

### 15. 📌 الترويج — ✅
التحقق من **جدول الأسعار الدقيق** من الـ API: تمييز 3 أيام=30 / 7=70، تثبيت 3=36 / 7=84، تثبيت+تمييز 3=66 / 7=154 (=مجموع الاثنين). خطط الرؤية، عرض السعر (quote)، goal غير صالح→400، بدء boost (بدون توكن→401، وبوابة الدفع خارجية).

### 16. 🚫 الحظر والإبلاغ — ✅
حظر/إلغاء حظر + قائمة المحظورين، إبلاغ عن منشور، إبلاغ بهدف ناقص→400.

### 17. ⚙️ الإعدادات والخصوصية — جزئي
- **✅**: change-password (§1)، إعدادات الحساب `GET/PATCH /api/users/me/account`، الخصوصية `GET/PATCH /api/users/me/privacy` (موجودة). الشاشات موجودة (`profile/settings/*`).
- **N/A / FAIL محتمل**: **حذف الحساب غير موجود** (لا مسار ولا API ولا شاشة) — يُصنّف N/A لعدم وجوده؛ إن كان مطلوبًا فهو فجوة ميزة.

### 18. 🆘 المساعدة والدعم — ✅
meta + FAQs عامة، إنشاء تذكرة + قائمتها + رد، بدون توكن→401، طلب التوثيق. (كشف واختبر عطل الانهيار المذكور أعلاه.)
- **N/A**: إغلاق التذكرة من جهة المستخدم غير موجود (إغلاق من الأدمن فقط).

### 19. 🐪 خدمات سرح — ✅
قائمة الخدمات الرسمية عامة عبر `GET /api/services` وتصيير شاشة `/sarh-services`.
- **N/A**: `GET /api/services/:id` غير موجود (قائمة فقط)؛ الروابط الخارجية تُفتح خارج التطبيق.

### 20. 🧭 التنقل والـ UI/RTL — ✅
`dir=rtl` و`lang=ar`، تصيير مسارات التبويب السفلي، وتحميل مسارات الميزات العامة دون خطأ خادم.
- **N/A**: تفاصيل بصرية دقيقة (اتجاه الأسهم، مواضع الأيقونات) تحتاج فحص بصري؛ خارج نطاق E2E الآلي هنا.

### 21. 🌐 الشبكة والـ API — ✅
رموز الأخطاء: 400 (تحقق)، 401 (بدون مصادقة)، 403 (مستخدم عادي على مسار أدمن)، 404 (مورد/مسار غير موجود)، health. Token منتهي/غير صالح (§1).
- **N/A**: 429 (rate-limit معطّل في وضع التطوير)، 409/422 لا تُستخدم كرموز قياسية هنا، محاكاة الإنترنت البطيء.

### 22. 📱 حالات التطبيق — جزئي
- **✅**: التمهيد لأول مرة، تحميل البيانات، حالات النجاح/الخطأ (عبر تصيير الشاشات والـ API).
- **N/A**: Background↔Foreground، تدوير الشاشة، الضغط المزدوج — سلوكيات أصلية للجهاز.

### 23. 🔒 الصلاحيات والأمان — ✅
مستخدم عادي لا يصل لمسارات الأدمن (403)، لا يعدّل/يحذف إعلان/منشور/ملف غيره (403/404)، القوائم الذاتية بدون توكن→401، رفض دخول الأدمن ببيانات مستخدم عادي.

### 24. 🖥️ لوحة الإدارة — ✅
تسجيل الدخول (وبيانات خاطئة تبقى في صفحة الدخول)، لوحة الإحصائيات الحية، **كل أقسام الشريط الجانبي الـ16** تُفتح دون انهيار، محتوى الإعلانات/المنشورات/الباقات/الطلبات/الدعم، البحث والفلاتر (مستخدمون/طلبات/باقات/دعم)، إعادة التوجيه لصفحة الدخول بدون جلسة، تسجيل الخروج.
- **N/A**: صفحة "مدفوعات/ترويج" مستقلة غير موجودة (الترويج داخل التطبيق، والمدفوعات عبر الباقات/الطلبات).

### 25. 🧨 حالات الحواف — ✅
نص فارغ، نص طويل جدًا، سعر سالب/صفر/ضخم، صور أكثر من الحد، Emoji + عربي/إنجليزي مختلط (يُقبل)، أحرف خاصة/حقن SQL في البحث (لا انهيار)، جسم غير صالح→400.
- **N/A**: صورة ضخمة فعلية، إغلاق التطبيق/الرجوع أثناء الطلب (سلوك أصلي/واجهة).

---

## ملاحظات N/A (ميزات غير موجودة أو خارج نطاق E2E الآلي)

- **حذف الحساب**: غير موجود في التطبيق أو الـ API.
- **بث مباشر (broadcast)**: معطّل بعلم ميزة (`LIVE_BROADCAST_ENABLED=false`) — المشاهدة تعمل.
- **بوابة الدفع N-Genius**: طرف ثالث غير متاح من بيئة الاختبار؛ منطق التطبيق واختبار الأخطاء المنظّمة مغطّى.
- **الرسائل اللحظية / Socket.IO**، **Push**، **دورة حياة التطبيق الأصلية (background/rotation)**: تعتمد على منصة أصلية/اتصال socket ولم تُدرَج في هذه الجولة الآلية.
