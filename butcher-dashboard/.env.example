# Public API origin (not a secret). Used by Next rewrites AND inlined for any
# server-side fetch. Browser XHR goes to same-origin /api and is proxied.
NEXT_PUBLIC_API_URL=http://127.0.0.1:3001

# Public Socket.IO origin (not a secret). Browser connects here directly;
# Render Socket CORS must allow this dashboard's https origin.
NEXT_PUBLIC_SOCKET_URL=http://127.0.0.1:3002

# --- Vercel production (set in the Vercel project, Root Directory = butcher-dashboard) ---
# NEXT_PUBLIC_API_URL=https://sarh-new4.onrender.com
# NEXT_PUBLIC_SOCKET_URL=https://sarh-socket.onrender.com
#
# Do not put JWT secrets, DB URLs, or Redis URLs in NEXT_PUBLIC_*.
# Do not assume a custom domain (alsfat.com / dashboard.sarh.app) until DNS is requested.
