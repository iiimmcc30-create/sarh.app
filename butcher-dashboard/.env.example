# NestJS API origin used by the Next.js rewrite proxy (server-side).
NEXT_PUBLIC_API_URL=http://127.0.0.1:3001
# Existing Socket.IO process (default local port 3002). Dashboard itself runs on 3003.
NEXT_PUBLIC_SOCKET_URL=http://127.0.0.1:3002
